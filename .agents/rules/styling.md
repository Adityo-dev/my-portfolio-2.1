# Global Styling Rules

1. Always follow the padding, margin, gap, and text size flow established in "ProjectsSection.tsx".
2. For buttons, use 'rounded-sm' for border radius instead of fully rounded, matching the ActionButton component.
3. Keep text formatting and typography consistent with the rest of the dark premium UI.
